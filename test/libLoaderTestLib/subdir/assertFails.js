/**
 * The purpose of this module is to confirm that the moduleLoader loads modules lazily.
 * If it loads all modules during construction then at least one test should fail.
 */
var assert = require('assert');
assert(false);
